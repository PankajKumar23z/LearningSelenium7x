package com.thetestingpractise.ex_selenium;

public class Selenium07 {

    public static void main(String[] args) {

        // EdgeDriver driver = new EdgeDriver();
    // Hover on EdgeDriver and click on Ctrl we can see below

        // EdgeDriver(C) extends ChromiumDriver
        // ChromiumDriver(C) extends RemoteWebDriver
        // RemoteWebDriver(C) extends  WebDriver
        // WebDriver(I) extends SearchContext
        // SearchContext(I)has only 2 functions



    }
}
